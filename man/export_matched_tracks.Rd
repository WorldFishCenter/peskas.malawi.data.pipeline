% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_matched_tracks}
\alias{export_matched_tracks}
\title{Export Matched Vessel Tracks with Landing Data}
\usage{
export_matched_tracks(log_threshold = logger::DEBUG)
}
\arguments{
\item{log_threshold}{Logging threshold level (default: logger::DEBUG)}
}
\value{
None. Processes data and uploads results to MongoDB.
}
\description{
Retrieves merged landing and vessel tracking data from MongoDB, processes track points
in parallel, joins with landing data, and exports the combined dataset back to MongoDB.
The function handles the entire pipeline from data retrieval to aggregation and storage.
}
\details{
The function performs these main operations:
\enumerate{
\item Retrieves validated landings data from MongoDB
\item Extracts unique trip IDs
\item Fetches vessel tracking points in parallel using the PDS API
\item Joins tracking data with landing information
\item Processes and aggregates the combined data:
\itemize{
\item Removes unnecessary columns
\item Aggregates positions to 10-minute intervals
\item Calculates mean positions for each interval
}
\item Uploads the processed data to MongoDB
}
}
\note{
Requires appropriate MongoDB connection details and PDS API credentials in the
configuration file accessed via read_config().
}
\section{Data Processing}{

The function aggregates tracking data by:
\itemize{
\item Rounding timestamps to 10-minute intervals
\item Computing mean positions (latitude/longitude) for each interval
\item Grouping by trip, vessel, and catch information
}
}

\examples{
\dontrun{
# Export tracks with default settings
export_matched_tracks()

# Export tracks with custom logging level
export_matched_tracks(log_threshold = logger::INFO)
}

}
\seealso{
\code{\link{read_config}} for configuration handling
\code{\link{get_trip_points}} for PDS API interaction
}
\keyword{export}
\keyword{workflow}
